import { init, exit } from "myPackage";
init({
    url: "true",
});
exit(1);
const hello = () => "hi";
class Block {
    constructor(data) {
        this.data = data;
    }
    static hello() {
        return "hi";
    }
}
